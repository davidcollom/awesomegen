{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://example.com/schemas/awesomegen/config-schema.json",
  "$defs": {
    "Category": {
      "properties": {
        "name": {
          "type": "string",
          "description": "Category name"
        },
        "items": {
          "items": {
            "$ref": "#/$defs/Item"
          },
          "type": "array",
          "description": "Array of items in this category - Manually set if required"
        },
        "version": {
          "type": "integer",
          "description": "Configuration version"
        },
        "user": {
          "type": "string",
          "description": "User identifier"
        },
        "lists": {
          "items": {
            "$ref": "#/$defs/List"
          },
          "type": "array",
          "description": "Array of lists"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "items",
        "version",
        "user",
        "lists"
      ]
    },
    "Item": {
      "properties": {
        "type": {
          "$ref": "#/$defs/ItemType",
          "description": "Item type - either github repository or link"
        },
        "repo": {
          "type": "string",
          "description": "Repository in owner/repo format (for github items)"
        },
        "url": {
          "type": "string",
          "description": "URL (for link items)"
        },
        "title": {
          "type": "string",
          "description": "Item title"
        },
        "notes": {
          "type": "string",
          "description": "Additional notes"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "type"
      ]
    },
    "ItemType": {
      "type": "string",
      "enum": [
        "github",
        "link"
      ]
    },
    "List": {
      "properties": {
        "slug": {
          "type": "string",
          "description": "List slug identifier"
        },
        "sources": {
          "items": {
            "$ref": "#/$defs/Source"
          },
          "type": "array",
          "description": "Array of additional sources to include"
        },
        "title": {
          "type": "string",
          "description": "List title"
        },
        "tagline": {
          "type": "string",
          "description": "List tagline"
        },
        "output": {
          "type": "string",
          "description": "Output path or format"
        },
        "min_stars": {
          "type": "integer",
          "description": "Minimum stars required to be included"
        },
        "stale_months": {
          "type": "integer",
          "description": "Number of months to consider stale"
        },
        "badges": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Array of badge names"
        },
        "categories": {
          "items": {
            "$ref": "#/$defs/Category"
          },
          "type": "array",
          "description": "Array of categories"
        },
        "group_by_topic": {
          "type": "boolean",
          "description": "Group repositories by GitHub topic (default: false)",
          "default": false
        },
        "topic_fallback": {
          "type": "string",
          "description": "Fallback topic name if none found (default: 'misc')",
          "default": "misc"
        },
        "topic_grouping_mode": {
          "$ref": "#/$defs/TopicGroupingMode",
          "description": "Topic grouping style: 'flat' or 'nested' (default: 'flat')"
        },
        "stars_format": {
          "$ref": "#/$defs/StarsFormat",
          "description": "Format for displaying stars: 'locale'"
        },
        "locale": {
          "type": "string",
          "description": "Locale for formatting numbers"
        },
        "group_by_top_tags": {
          "type": "boolean",
          "description": "Group repositories by top tags"
        },
        "top_tags_limit": {
          "type": "integer",
          "description": "Maximum number of top tags to group by"
        },
        "min_tag_count": {
          "type": "integer",
          "description": "Minimum count for a tag to be considered a top tag"
        },
        "single_home": {
          "type": "boolean",
          "description": "If true"
        },
        "tag_aliases": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Map of tag aliases for normalization"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "slug",
        "sources",
        "title",
        "tagline",
        "group_by_topic",
        "topic_fallback",
        "topic_grouping_mode",
        "stars_format",
        "locale",
        "group_by_top_tags",
        "top_tags_limit",
        "min_tag_count",
        "single_home",
        "tag_aliases"
      ]
    },
    "Source": {
      "properties": {
        "user": {
          "type": "string"
        },
        "slug": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "user",
        "slug"
      ]
    },
    "StarsFormat": {
      "type": "string",
      "enum": [
        "locale",
        "compact",
        "none"
      ]
    },
    "TopicGroupingMode": {
      "type": "string",
      "enum": [
        "flat",
        "nested"
      ]
    }
  },
  "$comment": "This schema is auto-generated from the Go struct definitions. Do not edit directly.",
  "properties": {
    "version": {
      "type": "integer"
    },
    "user": {
      "type": "string"
    },
    "lists": {
      "items": {
        "$ref": "#/$defs/List"
      },
      "type": "array"
    }
  },
  "additionalProperties": false,
  "type": "object",
  "required": [
    "version",
    "user",
    "lists"
  ],
  "title": "Config.YAML (Generated: 2025-11-07 16:06:22)",
  "description": "Configuration schema for awesomegen"
}